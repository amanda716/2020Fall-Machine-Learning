# 调用千问模型（Qwen）示例

如果你希望“既能真实调用千问模型，又能执行自动化测试”，推荐用仓库中的脚本：`qwen_call.py` + `tests/test_qwen_call.py`。

## 1) 安装依赖

```bash
pip install openai python-dotenv
```

## 2) 配置 API Key（真实调用需要）

```bash
export DASHSCOPE_API_KEY="你的APIKey"
```

> API Key 所属地域使用国际站时，Base URL 建议：`https://dashscope-intl.aliyuncs.com/compatible-mode/v1`。

## 3) 真实调用千问模型

### 方式 A：使用环境变量（推荐）

```bash
python qwen_call.py --prompt "你好，请用一句话介绍你自己"
```

### 方式 B：命令行直接传 key

```bash
python qwen_call.py \
  --api-key "你的APIKey" \
  --base-url "https://dashscope-intl.aliyuncs.com/compatible-mode/v1" \
  --model "qwen-plus" \
  --prompt "请用一句话介绍机器学习"
```

## 4) 执行自动化测试（不访问网络）

下面测试会 mock 掉 OpenAI 客户端，不会真实调用外部接口：

```bash
python -m unittest discover -s tests -p 'test_*.py' -v
```

## 5) 可选：语法检查

```bash
python -m compileall qwen_call.py tests
```
